import { h } from "preact";

function MenuOptions() {
  return (
    <div>
      <a href="#">Share on Twitter</a>
      <a href="#">About Us</a>
      <a href="#">Logout</a>
    </div>
  );
}

export default MenuOptions;
